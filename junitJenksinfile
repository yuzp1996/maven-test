pipeline {
  agent any
  
  stages{
    stage("build"){
      steps{
        sh 'rm -rf ./*'
        sh 'mv ./example/* ./'
        sh 'mvn test'
      }
    }
  }
  
      post {
        always {
            junit "**/target/surefire-reports/**/*.xml"
        }
    }
}
