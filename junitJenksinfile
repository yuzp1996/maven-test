pipeline {
  agent any
  
  stages{
    stage("build"){
      steps
      sh 'mv ./example/* ./'
        sh '/apache-maven-3.5.4/bin/mvn package'
      }
    }
  }
  
      post {
        always {
            junit "**/target/surefire-reports/**/*.xml"
        }
    }
}
