pipeline {
  agent any
  
  stages{
    stage("build"){
      steps{
        sh 'mv ./example/* ./'
        sh 'mvn test'
      }
    }
  }
  
      post {
        always {
            junit "**/target/surefire-reports/**/*.xml"
            sh 'rm -rf ./*'
        }
    }
}
